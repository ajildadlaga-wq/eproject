# Copy to .env and fill in. For local Supabase (`supabase start`), the CLI
# prints these values; the anon key is the same well-known local dev key.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-local-or-hosted-anon-key
